services:
  chromadb-test:
    image: chromadb/chroma:0.5.0
    container_name: chromadb-test
    ports:
      - "8001:8000"  # Use different port to avoid conflicts
    environment:
      - ANONYMIZED_TELEMETRY=false
      - CHROMA_SERVER_HOST=0.0.0.0
      - CHROMA_SERVER_HTTP_PORT=8000
      - PERSIST_DIRECTORY=/chroma/chroma
    volumes:
      - chromadb-test-data:/chroma/chroma
    networks:
      - test-network
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8000/api/v1/heartbeat || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  retrieve-test:
    build:
      context: .
      dockerfile: cmd/retrieve/Dockerfile
    container_name: retrieve-test
    ports:
      - "8081:8081"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - CHROMA_URL=http://chromadb-test:8000
      - CHROMA_COLLECTION_NAME=test_demo_collection
      - METADATA_DB_PATH=/app/test_metadata.db
      - LOG_LEVEL=debug
    volumes:
      - ./tests/integration/testdata:/app/testdata:ro
      - test-metadata:/app
    networks:
      - test-network
    depends_on:
      chromadb-test:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s

  # SQLite doesn't need a separate service - it's file-based
  # Test metadata is created in the mounted volume

volumes:
  chromadb-test-data:
    driver: local
  test-metadata:
    driver: local

networks:
  test-network:
    driver: bridge
